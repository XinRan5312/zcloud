spring:
  application:
    name: supervision-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka/
server:
  port: 8084

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS